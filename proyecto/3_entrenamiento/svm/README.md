# Entrenamiento SVM

Scripts de entrenamiento y ajuste de hiperparámetros del modelo Support Vector Machine (kernel lineal y RBF, valores de C). Guarda los reportes de clasificación (.txt) y las curvas de exactitud por configuración (.png).
